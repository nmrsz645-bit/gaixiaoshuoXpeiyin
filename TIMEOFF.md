# 小说处理中心工作交接

更新时间：2026-08-31（Asia/Shanghai）  
项目根目录：`E:\自动化\改小说+配音`

> 本文是当前工作的唯一交接入口。接手者无需阅读历史聊天，但必须先完整阅读本文。线上正式版本已为 **1.0.15**：新增“超 59 分钟 MP3 自动保留前 35 分钟”，已完成独立构建、递归包审计、真实媒体验证、隔离升级/回滚、激活后三入口公网回读。现有配置、数据、日志、程序和 1.0.14 记录均保留；后续发布必须以新版本号和同等级验证进行，禁止覆盖既有版本文件。

## 接手首要状态（以本节为准）

- 权威源码：`E:\自动化\改小说+配音` 根目录与 `novel_monitor\`。线上当前版本为 `1.0.15`，不应重复发布。
- Git：本地与远端 `main` 已完成首次推送，首个提交为 `75ff2e0`，远端为 `https://github.com/nmrsz645-bit/gaixiaoshuoXpeiyin.git`。用户配置、数据、日志和密钥均未进入仓库。
- 新电脑开发接手：在 Windows + Python 3.13 环境克隆仓库、安装 `requirements.txt`、执行 `运行回归测试.bat`，并在 GitHub Actions 确认 `Offline regression` 绿灯。该测试不使用真实 Key 或收费接口。
- 新电脑直接使用：使用已发布的 `1.0.15` 完整包；Git 不迁移用户 Key、配置、小说、MP3、日志和队列。新机器必须自行配置，或经用户明确同意后安全迁移这些数据。
- 旧段落中的 `1.0.14`/`64 项测试`均为历史证据；当前回归基线为 **66 项**，当前发布版本为 **1.0.15**。

## 2026-08-31 Git 交接与离线回归（已完成）

- 权威源码 `E:\自动化\改小说+配音` 已初始化 Git，默认分支为 `main`，远端为 `https://github.com/nmrsz645-bit/gaixiaoshuoXpeiyin.git`；已使用仅限本仓库的 Git 作者配置完成首次提交与推送。
- 新增 `.gitignore`，明确排除真实配置、Key、Webhook、规则、小说、音频、日志、回执、统计、构建/发布/隔离验证目录和历史备份；已审计暂存源码清单，敏感 Key 特征扫描为 0。
- 新增 `README.md`、`AGENTS.md`、`.env.example`，新电脑接手必须先读这三份文件和本文。
- 新增 `run_tests.bat`（英文稳定入口）、`运行回归测试.bat`（中文兼容入口）与 `.github/workflows/offline-regression.yml`：Windows/Python 3.13 下执行语法检查与 `python -m unittest test_pipeline.py`。CI 仅跑离线测试，不读取密钥、不调用真实 AI/TTS、不上传 OSS 或发布更新。
- 已优化交接表述：后文 2026-08-21 的记录均已显式标为历史快照，不能再用其中的 `1.0.14`、`64 项测试`或“不是 Git 仓库”判断当前状态；当前状态只以本文顶部和本节为准。
- 已做全新克隆实测：新目录安装 `requirements.txt` 后，`run_tests.bat` 与 `运行回归测试.bat` 均为 66 项通过，`unified_app`、`voice_monitor` 可导入；最新 CI Job `99284043811` 也已成功。测试未填写 Key，未调用真实 AI/TTS。

## 2026-08-21 19:08 MP3 超时长自动裁剪正式发布（1.0.15，已完成）

- 已按确认规则实现：只处理新生成的配音 MP3；真实时长超过 `59:00`（3540 秒）时，只保留前 `35:00`（2100 秒），其余内容丢弃；59 分钟及以内完全不动；历史“完成”文件不回头修改。
- 改动位置：`voice_monitor.py`。裁剪只发生在 MP3 有效生成后、TXT 移入完成目录和成功回执删除前。
- 运行时使用 `imageio-ffmpeg` 随桌面包携带的 FFmpeg，不依赖用户电脑预装 FFmpeg；`requirements.txt` 已固定 `imageio-ffmpeg==0.6.0`，`构建桌面版.bat` 已要求 PyInstaller 收集该二进制。
- 失败保护：先把有效原 MP3 和 `audio_ready` 回执保存，再裁剪到唯一临时 MP3；只有时长复核为 35 分钟后才原子替换。裁剪失败会保留长 MP3、TXT 和回执，下一轮只重试裁剪，不会重新调用 Edge/阿里云配音。
- 自动化：新增“超时长裁到 35 分钟”和“裁剪失败后不重复合成”两项测试；完整回归为 `Ran 66 tests`、`OK`，语法检查通过。
- 真实隔离媒体：生成 59 分 1.06 秒的真实 MP3，完整走配音成功收尾后得到 35 分 0.05 秒有效 MP3；TXT 已正确进入隔离完成目录，合成调用次数为 1。证据：`E:\自动化\改小说+配音\隔离验证-MP3自动裁剪-20260821\真实MP3自动裁剪验证结果.json`。
- 正式候选源：`E:\自动化\改小说+配音\发布候选-MP3自动裁剪-1.0.15-20260821-r2\小说处理中心`；独立发布工作目录：`E:\自动化\gengxin\_build-xiao-shuo-chu-li-zhong-xin-1.0.15`。更新包 1005 条目、完整包 1008 条目；受保护配置/数据/日志条目和疑似密钥条目均为 0。
- 运行时验证：完整包携带 `_internal\imageio_ffmpeg\binaries\ffmpeg-win-x86_64-v7.1.exe` 和内置更新器；隔离界面实际启动并保持运行。空白新安装未填写 DeepSeek Key 时会提示配置缺失，这是既有首次配置边界；真实升级验证证明已有用户的 Key 与所有配置不受影响。
- 真实升级与回滚：公网 `1.0.13` 隔离升级到 `1.0.15` 成功；20 项受保护配置、规则、队列、输出、失败项、日志和用户文件 SHA-256 均未变；9 项必需文件（含随包 FFmpeg）齐全；安装目录内测试进程被正确停止、外部进程保留；生成 `app.previous`；手工回滚后恢复 `1.0.13`，20 项数据仍不变。
- 已激活并公网回读：主 `latest.json`、旧兼容 `1.0.0/latest.json` 和 `downloads/catalog.json` 均为 `1.0.15`。更新包：`https://luotuoruanjiangengx.oss-cn-beijing.aliyuncs.com/updates/xiao-shuo-chu-li-zhong-xin/1.0.15/app.zip`，57,123,213 字节，SHA-256 `83BAAA7E30D9FBCFF69580DF6707D99B37D51A38204F6FE75BFDE02AFB8AF527`；完整包：`https://luotuoruanjiangengx.oss-cn-beijing.aliyuncs.com/packages/xiao-shuo-chu-li-zhong-xin-1.0.15.zip`（下载站别名同文件），57,144,487 字节，SHA-256 `7D4FCFC3963E72D39FFAA3596F6B4CA3D0A36073B3813906F8C1DF624BC7DBB9`。
- 正式根 `config.json` SHA-256 仍为 `BA500140B4A8BAB1C398518C4BE70D7850E09AE3D7EE44D4CF278F9A770C45DD`，`改写\config.json` 仍为 `667AD4BE34E66840140CA15645173BE7F1EB74DFD2D77EC3BEF7E53C1A4366D1`，正式 DeepSeek Key 仍为 0 字节。

## 2026-08-21 04:46 正式发布（已完成）

- 正式版本：`1.0.14`。
- 唯一正式候选源：`E:\自动化\改小说+配音\发布候选-DeepSeek官方-1.0.14-20260821-r2\小说处理中心`。
- 独立发布工作目录：`E:\自动化\gengxin\_build-xiao-shuo-chu-li-zhong-xin-1.0.14`。
- 更新包：`https://luotuoruanjiangengx.oss-cn-beijing.aliyuncs.com/updates/xiao-shuo-chu-li-zhong-xin/1.0.14/app.zip`
  - 大小：`24,950,563` 字节。
  - SHA-256：`272EB3493CDC3903BD32A80CBB89D90ACD68DD479D1A3676A2F9E72A9697540E`。
- 完整包：`https://luotuoruanjiangengx.oss-cn-beijing.aliyuncs.com/packages/xiao-shuo-chu-li-zhong-xin-1.0.14.zip`
  - 下载站别名：`https://luotuoruanjiangengx.oss-cn-beijing.aliyuncs.com/downloads/packages/xiao-shuo-chu-li-zhong-xin-1.0.14.zip`。
  - 大小：`24,971,783` 字节。
  - 两个公网入口 SHA-256 均为：`53693C5CA3F3B46E338C3185B8AB8D8B2900C7F85EB12C63092BBD1DCE09057D`。
- 激活后独立公网回读：主 `latest.json`、旧兼容 `1.0.0/latest.json`、`downloads/catalog.json` 均为 `1.0.14`；更新包、根完整包和下载站完整包重新下载后的 SHA-256 均与发布记录一致。
- 包审计：更新包 1003 个条目、完整包 1006 个条目；受保护配置/数据/日志/任务/输出条目为 0，疑似密钥条目为 0。
- 完整包隔离启动：`Start.cmd` 可启动新程序；程序版本为 `1.0.14`；根更新器和内置更新器 SHA-256 均为 `59E3FD575350C274E2134168D7F61C3CACA8AC61377570AF92E5A559F54D1241`。
- 真实更新与回滚：从公网 `1.0.13` 更新到 `1.0.14` 成功；20 项受保护用户数据保持不变；8 项必需文件齐全；生成 `app.previous`；只结束安装目录内测试进程，外部测试进程保留；回滚后版本恢复为 `1.0.13` 且 20 项数据仍不变。
- 临时 DeepSeek 实测：生产代码使用官方 URL 和 `deepseek-v4-flash` 完成 430 字、8 个非空段落的真实请求；输入输出段落数一致，用时 2.6 秒，并确认关闭深度思考。临时 Key 只通过隐藏输入保存在测试进程内存中，未写入配置、证据、源码、日志或发布包；正式 `改写\deepseek_api_key.txt` 仍为 0 字节。
- 已通知 Codex 任务 `019fdfd3-80e4-7622-9be9-7867bad946a5`（标题“更新2.0”）同步发布记录；该任务已回执确认记录完整、无需修改且不会重复发布。

## 2026-08-21 05:15 双任务真实并行改写（已完成）

- 使用 `1.0.14` 同一套生产函数 `novel_monitor.deepseek_client.optimize_text`，以两个工作线程同时调用 DeepSeek 官方 `deepseek-v4-flash`。
- 甲、乙两份合成小说各 1486 字、8 个非空段落，各自实际拆分并完成 `2/2` 请求。
- 两任务实际重叠执行 5.7 秒，总耗时 5.8 秒，证明不是串行测试。
- 两份输出均为 1486 字、8 个非空段落；甲任务中的乙任务专属人物/文档/地点标记为 0，乙任务中的甲任务专属标记也为 0，没有串书或内容混入。
- 临时 Key 仅通过隐藏输入进入测试进程；测试目录疑似 Key 扫描为 0，正式 `改写\deepseek_api_key.txt` 仍为 0 字节。
- 证据：`E:\自动化\改小说+配音\隔离验证-双任务并行-DeepSeek官方-1.0.14-20260821\双任务真实并行验证结果.json`，SHA-256：`820A451407AF861C473DC415425C55A70CC4EFA4BFF21FA0B1034758D5D078E8`。

## 2026-08-21 04:25 接手复验（已完成）

已按交接要求先复跑完整自动化测试：

```powershell
Set-Location -LiteralPath 'E:\自动化\改小说+配音'
python -m unittest test_pipeline.py
```

结果：`Ran 64 tests in 0.388s`、`OK`。测试中故意注入的 `ERROR`/失败日志符合预期。测试完成前没有构建、发布或修改线上 `latest.json`/`catalog.json`。

随后完成：

1. `python -m py_compile novel_monitor\config.py novel_monitor\deepseek_client.py novel_monitor\processor.py unified_app.py`，退出码 0。
2. 只读回查三份既有验证报告，均存在且 JSON 结论仍为成功：
   - DeepSeek 官方 18,306 字实跑：623 个非空段落输入输出一致、3 次请求均关闭深度思考、80.8 秒。
   - 既有改写到配音实跑：2/2 改写、2/2 配音成功，失败队列为 0。
   - UI 设置：`DeepSeek官方`、官方 URL、`deepseek-v4-flash`、8000 字、300 秒均正确。
3. 再次运行无外网 UI 实例验证，断言通过。界面初始化刷新了根 `config.json` 和 `改写\config.json` 的写入时间，但两文件长度和 SHA-256 均未变化；已把写入时间精确恢复到验证前值。
4. 核对运行中自动更新的源码层兼容性：
   - 更新入口仍只在安装目录名为 `app` 时启用，并把内置补丁安装到同级 `updater` 后执行 `UpdateAgent.exe --silent --check`。
   - 根目录与旧候选内的 `UpdateAgent.exe`、`updater-config.json` 逐文件 SHA-256 一致；`UpdateAgent.exe` 为 `59E3FD575350C274E2134168D7F61C3CACA8AC61377570AF92E5A559F54D1241`。
   - 两套既有隔离升级目录均存在 `app.previous`；升级前后 `config.json` 和 `user-custom\keep.txt` 的 SHA-256 分别一致。
   - 本轮新源码尚未构建，因此以上只证明源码调用和补丁资产兼容；新候选的真实运行中更新仍必须在获准构建后复验，不能把旧候选当作本轮候选。
5. 对正式配置、规则、队列、输出、失败目录、随机音色、试听、日志和统计做验证前后比对；9 个现有受保护文件内容未改变，正式程序未启动，正式凭据仍为空。唯一重新写入的业务外证据是隔离目录内 `UI设置验证结果.json`，其内容哈希与既有结果一致。

## 2026-08-21 历史目标（非当前状态）

把“改小说 + 自动配音”统一程序的改写接口正式调整为以 **DeepSeek 官方接口**为主，同时保持：

1. 程序内可选择 `DeepSeek官方` 或 `自定义` AI 服务。
2. DeepSeek 官方默认地址为 `https://api.deepseek.com/chat/completions`，默认模型为 `deepseek-v4-flash`。
3. 默认每次请求最多 8000 字、请求等待上限 300 秒（5 分钟），均可在程序界面修改。
4. 官方 DeepSeek 请求显式关闭深度思考，避免改写变慢和输出长度被思考内容占用。
5. 长文本先按安全长度分段；某段返回不完整、编号缺失或达到输出上限时，只对失败段自适应拆小并重试，不让整本书从头重跑。
6. 保持既有改写到配音、Edge 优先/阿里云回退、事务恢复、失败重试、24 小时日志、每日统计、无人值守和运行中自动更新能力不被破坏。
7. 已生成可供其他全新 Windows 电脑使用的正式完整包和自动更新包，并在保留 `1.0.14` 的前提下发布当前 `1.0.15`。

## 2026-08-21 已完成的源码修改（历史记录）

### 1. AI 配置和默认值

- `E:\自动化\改小说+配音\novel_monitor\config.py`
  - 官方默认 URL 改为 `https://api.deepseek.com/chat/completions`。
  - `AppConfig` 新增默认配置：
    - `max_request_chars = 8000`
    - `request_timeout_seconds = 300`
  - 从配置读取 `maxRequestChars`、`requestTimeoutSeconds`。

- `E:\自动化\改小说+配音\改写\ai_api_url.txt`
  - 当前内容为 DeepSeek 官方完整接口地址。

- `E:\自动化\改小说+配音\改写\config.json`
  - 已保存：`deepseekModel = deepseek-v4-flash`、`maxRequestChars = 8000`、`requestTimeoutSeconds = 300`。
  - 原有输出目录、失败目录、日志目录等用户路径没有重置。
  - `aiUrl` 字段当前为空，实际 URL 由 `改写\ai_api_url.txt` 提供，这是当前程序原有的文件式配置机制，不要仅看 JSON 就误判 URL 未配置。

### 2. DeepSeek 请求和长文容错

- `E:\自动化\改小说+配音\novel_monitor\deepseek_client.py`
  - 默认请求长度 8000 字、最多 250 个段落、超时 300 秒。
  - 官方域名请求加入 `thinking: {"type": "disabled"}`。
  - `optimize_text` 接受可配置的 `max_chars`。
  - 返回达到长度上限、段落编号缺失/重复、输出长度异常等情况会触发失败段递归拆半，不重做已成功段。
  - 分段检查点缓存版本已更新为 4，避免误用旧缓存。

- `E:\自动化\改小说+配音\novel_monitor\processor.py`
  - 第一轮和违禁词第二轮改写都会传入界面保存的最大请求字数和超时。

### 3. 程序界面

- `E:\自动化\改小说+配音\unified_app.py`
  - “改小说设置”增加：AI 服务、API Key、AI 地址、模型、单次最大字数、等待上限、企业微信 Webhook。
  - AI 服务可选 `DeepSeek官方` / `自定义`。
  - 选择 `DeepSeek官方` 自动填入官方 URL、`deepseek-v4-flash`、8000 字、300 秒。
  - 参数校验范围：单次字数 1000–20000；超时 30–600 秒。
  - “测试全部接口”对官方 DeepSeek 同样显式关闭深度思考。

### 4. 测试和说明

- `E:\自动化\改小说+配音\test_pipeline.py`
  - 当前共 64 项测试。
  - 已补官方 URL、默认 8000/300、关闭深度思考、动态分段等回归覆盖。

- `E:\自动化\改小说+配音\使用说明.txt`
  - 已补 DeepSeek 官方服务、模型、最大请求字数、5 分钟超时和自适应拆分说明。

## 2026-08-21 已完成并验证的内容（历史记录）

### 自动化验证

以下命令已经执行并通过：

```powershell
Set-Location -LiteralPath 'E:\自动化\改小说+配音'
python -m py_compile novel_monitor\config.py novel_monitor\deepseek_client.py novel_monitor\processor.py unified_app.py
python -m unittest test_pipeline.py
```

结果：语法检查通过；测试最终为 `Ran 64 tests`、`OK`。

配置加载也已验证：有效 URL 为 DeepSeek 官方接口，模型为 `deepseek-v4-flash`，单次 8000 字，超时 300 秒。

### DeepSeek 官方真实改写

已用用户临时提供的官方接口，对一份 18,306 字、623 个非空段落的真实文本运行修改后的生产代码：

- 初始拆为 3 段：7609 / 7272 / 3425 字。
- 实际请求 3 次。
- 3 次请求均确认关闭深度思考。
- 输出仍为 18,306 字、623 个非空段落，完整性校验通过。
- 用时 80.8 秒。

证据文件：

- `E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\DeepSeek官方改版实跑\验证结果.json`
- `E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\DeepSeek官方改版实跑\改写结果.txt`

### 单次请求长度阶梯测试

同一份真实文本分别测试了 6000、8000、10000、12000、14000 字。

DeepSeek 官方、关闭深度思考的结果：

| 单次字数 | 结果 | 用时 | 说明 |
|---:|---|---:|---|
| 6000 | 成功 | 28.5 秒 | 200 段完整 |
| 8000 | 成功 | 35.5 秒 | 262 段完整 |
| 10000 | 失败 | 41.5 秒 | 达到输出长度上限 |
| 12000 | 失败 | 41.3 秒 | 达到输出长度上限 |
| 14000 | 失败 | 40.9 秒 | 达到输出长度上限 |

因此当前默认值应继续保持 8000，而不是提高到 10000 以上。生产代码遇到单段失败还会进一步拆小。

证据：`E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\整段长度阶梯测试\DeepSeek官方\测试结果.json`

百炼接口也做过同样阶梯测试，结果仅用于比较，不是后续默认方案：6000、8000、12000、14000 成功，10000 因段落编号缺失失败。证据：`E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\整段长度阶梯测试\测试结果.json`。

### 界面验证

已真实创建 `App` 界面对象并验证控件值：

- AI 服务：`DeepSeek官方`
- URL：`https://api.deepseek.com/chat/completions`
- 模型：`deepseek-v4-flash`
- 单次最大字数：`8000`
- 等待上限：`300`
- 再次选择官方预设后仍能恢复上述值。

证据：`E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\UI设置验证结果.json`

完整窗口也曾真实启动；正式配置的 API Key 和 Webhook 为空时，自动启动会按设计提示缺少配置。Tk 控件无法被当时的截图自动化可靠识别，因此采用“真实实例化 + 读取控件变量”的方式完成验收。

### 改写到配音的完整实跑（本次 DeepSeek 改动之前）

在同一隔离目录中，以百炼改写 + 企业微信 + Edge 配音，真实跑完 2 本长小说：

- 改写 2/2 成功，共 71 段，0 个终态失败。
- 配音 2/2 成功，生成 2 个有效 MP3 和 2 个 TXT。
- 整体用时 1589 秒（26 分 29 秒）。
- 队列、处理中目录、改写失败和配音失败最终均为 0。
- MP3 大小：55,752,192 字节和 41,752,944 字节；此前已用 `ffprobe` 验证可解析，时长分别约 9292.032 秒、6958.824 秒。

证据：`E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\validation-summary.json`

注意：这证明现有配音链路可以完整运行，但 **DeepSeek 官方改动之后只做了真实改写实跑，没有再消耗时间完整配完一本真实长小说**。自动化测试中的约 4 万字改写到配音全链路已通过。

## 2026-08-21 历史状态（非当前状态）

- 源码：已修改。
- 自动化测试：已通过 64 项。
- DeepSeek 官方真实接口：已完成一次 18,306 字真实改写并通过。
- UI 配置：已完成控件级真实实例验证。
- 本次接手复验：64 项测试、语法检查、三份报告回查和无外网 UI 验证均通过。
- 自动更新：已完成公网 `1.0.13 -> 1.0.14` 的包级更新、用户数据保持、进程边界、`app.previous` 和回滚验证。
- 正式根目录凭据：已恢复为空文件。
- 运行状态：小说处理主程序当前未运行；只有记事本打开了一个隔离验证文本。
- Git：该目录不是 Git 仓库，不能依赖 `git status` 或 Git 回滚。
- 构建：已在全新 r2 候选目录完成，未覆盖任何旧发布目录或回滚目录。
- 版本号：正式发布版本为 `1.0.14`。
- 发布：更新包、完整包已上传并通过公网哈希回读；主清单、旧兼容清单和下载目录已激活为 `1.0.14`；指定更新任务已收到通知。

## 2026-08-21 历史未完成事项

1. 用户实际使用前，需要在程序“改小说设置”中填写自己的 DeepSeek 官方 Key 并保存；更新器会保留旧配置，不会静默覆盖用户凭据。
2. 本次测试使用的是用户明确提供的临时 Key，程序及发布证据均未保存。测试目的已经完成，建议用户在 DeepSeek 后台撤销或轮换该临时 Key。
3. 如需更强的真实验收，可在隔离目录再跑一本小说并接着跑 Edge/阿里云配音；这不是 1.0.14 发布阻塞项，会消耗接口费用和较长时间，不能把测试文件投入正式队列。

## 建议的后续具体操作顺序

1. 不要重复发布 `1.0.14`。需要复核时，优先只读检查线上清单、下载包 SHA-256 和本节记录。
2. 用户端打开旧版后，可让运行中更新器检查并升级到 `1.0.14`；全新电脑可使用上述完整包。
3. 用户填写 DeepSeek 官方 Key 后，先在界面运行“测试全部接口”，再投入正式小说队列。
4. 下一次修改必须从项目根目录源码开始，使用新的版本号和新的隔离候选目录；不得把本次候选、工作目录或 `app.previous` 当作可覆盖的临时目录。

## 关键文件与路径

### 当前源码（唯一应继续修改的工作源）

- `E:\自动化\改小说+配音\unified_app.py`：统一 GUI、服务生命周期、接口测试、无人值守、运行中更新入口。
- `E:\自动化\改小说+配音\voice_monitor.py`：Edge/阿里云配音、领取/回执/失败重试、MP3 校验。
- `E:\自动化\改小说+配音\aliyun_tts.py`：阿里云 TTS。
- `E:\自动化\改小说+配音\novel_monitor\config.py`：改写配置读取和默认值。
- `E:\自动化\改小说+配音\novel_monitor\deepseek_client.py`：DeepSeek 请求、分段、协议校验、检查点和自适应重试。
- `E:\自动化\改小说+配音\novel_monitor\processor.py`：改写任务事务、Webhook、发布到配音队列。
- `E:\自动化\改小说+配音\novel_monitor\monitor_service.py`：改写监控服务。
- `E:\自动化\改小说+配音\novel_monitor\runner.py`：命令行监控入口。
- `E:\自动化\改小说+配音\test_pipeline.py`：完整回归测试。
- `E:\自动化\改小说+配音\使用说明.txt`：用户说明。

### 正式配置和用户数据

- `E:\自动化\改小说+配音\config.json`：配音/UI 主配置。
- `E:\自动化\改小说+配音\改写\config.json`：改写路径和参数配置。
- `E:\自动化\改小说+配音\改写\ai_api_url.txt`：AI 接口地址。
- `E:\自动化\改小说+配音\改写\deepseek_api_key.txt`：正式 AI Key，当前为 0 字节。
- `E:\自动化\改小说+配音\改写\wechat_webhook_url.txt`：正式企业微信 Webhook，当前为 0 字节。
- `E:\自动化\改小说+配音\改写\抖音广告合规规则.txt`：用户规则。
- `E:\自动化\改小说+配音\改写\指定违禁词.txt`：用户违禁词。
- `E:\自动化\改小说+配音\待改小说`：正式输入队列。
- `E:\自动化\改小说+配音\已改完成`：改写后/配音输入队列。
- `E:\自动化\改小说+配音\完成`：最终输出目录（用户也可在界面自定义）。
- `E:\自动化\改小说+配音\改写失败`、`E:\自动化\改小说+配音\配音失败`：终态失败任务。
- `E:\自动化\改小说+配音\每日统计.json`、日志、历史记录、回执和处理状态目录：7×24 恢复依据。

### 验证目录

- `E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821`
- 该目录包含真实测试脚本、输入/输出、验证报告和临时凭据，只能用于隔离验证，绝不能整体复制进发布包。

## 2026-08-21 历史已知问题和边界

1. **正式凭据为空**：`改写\deepseek_api_key.txt` 和 `改写\wechat_webhook_url.txt` 当前均为 0 字节。正式程序启动自动监控时会提示缺少配置，这是预期保护，不是本轮代码错误。用户需要在 GUI 中重新填写并保存。
2. **旧客户端配置不会自动切换**：更新器采用保留用户 `app` 数据的策略。已有用户如果仍保存百炼地址/Key，更新二进制后不会自动覆盖为 DeepSeek 官方配置。应让用户在“改小说设置”选择 `DeepSeek官方` 并填写官方 Key；不要静默覆盖用户凭据。
3. **请求长度上限**：真实结果显示官方 DeepSeek 在该小说和提示词下，10000 字以上容易达到输出上限。默认 8000 是实测后的安全值，但不是任何文本都绝对成功；失败段的自动拆分是必要兜底。
4. **未做改版后整本真实配音**：本轮官方 DeepSeek 修改后已真实完成改写，但没有再跑耗时的整本 TTS。配音代码本轮未修改，旧版真实全链路和自动化回归均通过。
5. **UI 自动化限制**：Tk 界面在当时的 Windows 自动化工具中不可稳定 OCR/定位标签，已改用真实实例化控件验证。若发版，仍建议人工目视一次完整界面。
6. **根目录不是 Git 仓库**：这是当日历史状态；当前根目录已初始化 Git，但尚未首提交，当前 Git 状态以本文顶部为准。
7. **历史目录很多且名称相近**：`发布版-*`、`发布候选-*`、`build-*` 不能凭“最新/最终”名称判断权威。当前源码权威位置就是项目根目录和 `novel_monitor` 子目录；新发布源必须由接手者在用户授权后明确命名。
8. **本轮已正式发布**：这是当日 1.0.14 记录；当前线上版本已为 1.0.15，发布哈希见本文顶部。不得在没有重新验证的情况下覆盖同版本文件或重复激活清单。

## 不能误动、不能打包、不能泄露的内容

1. 不得删除、移动、清空或覆盖用户的小说原文、改写结果、MP3、失败队列、历史记录、每日统计、日志、回执、处理中目录或自定义输出目录。
2. 不得覆盖 `改写\抖音广告合规规则.txt`、`改写\指定违禁词.txt`、随机音色选择、Edge/阿里云参数、企业微信设置和任何用户路径。
3. 不得修改或清空正式配置来“强制切换接口”；旧用户切换到 DeepSeek 官方应通过界面选择和填写 Key，除非用户另行明确授权迁移。
4. 不得把以下任何凭据写入源码、测试、TIMEOFF、日志、答复、发布包或上传文件：
   - `E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\整段长度阶梯测试\deepseek_official_api_key.txt`（含用户临时提供的 DeepSeek 官方明文测试 Key；用户表示后续会删除/撤销）。
   - `E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\改写\deepseek_api_key.txt`。
   - `E:\自动化\改小说+配音\隔离验证-优化后完整实跑-20260821\改写\wechat_webhook_url.txt`。
   - 任何阿里云 AccessKey ID/Secret、AppKey 或其他 API Key。
5. 不得把整个 `隔离验证-优化后完整实跑-20260821` 目录、任何 `源码备份-*`、旧 `发布版-*`、旧 `build-*`、`app.previous` 或用户运行数据并入新包。
6. 不得覆盖现有发布包或唯一回滚副本。新构建必须使用新的隔离输出目录。
7. 未经用户明确授权，不得上传 OSS、修改公网更新清单、通知更新会话、启动正式队列或调用会产生费用的真实接口。
8. 不要改动另一个可能存在的旧项目 `E:\自动化\改小说`；当前任务只属于 `E:\自动化\改小说+配音`。

## 发布验收底线（得到授权后适用）

正式发布必须同时满足：

1. 当前 66 项测试和语法检查通过（64 项为历史记录）。
2. 新候选目录构建成功，不能覆盖旧包。
3. 候选包递归审计无用户数据、配置、日志、任务、输出和凭据。
4. EXE 在没有 Python 的隔离环境可启动；真实 UI、配置保存和程序重启可用。
5. DeepSeek 官方改写、Edge 配音和阿里云回退至少完成相称的实际验收。
6. 运行中自动更新只结束本程序及其子进程，不得杀 Chrome、Node、PowerShell 等外部程序。
7. 隔离升级后用户数据不变、`app.previous` 存在且可回滚。
8. 更新包与完整包 SHA-256 已记录；公网文件、`latest.json`、兼容清单和 `catalog.json` 均逐项回读一致。
9. 只有以上全部通过，才可以告诉用户“已发布/已通知更新”。
